# QuickClips Personalization

This theme module provides a product personalization modal with live clip preview and Gemini-generated preview copy.

## UI Changes From Original

Original baseline commit: `f703355` (`Add modular theme personalization previewer with line item property sync`).

### Personalization modal updates

- Replaced the original 2-field text modal (`Primary`/`Secondary`) with:
  - style picker (`Style 1`, `Style 2`, `Style 3`)
  - `Name 1`
  - `Name 2`
  - `Date`
- Added style image tiles (`quickclip-style-1.jpg`, `quickclip-style-2.jpg`, `quickclip-style-3.jpg`).
- Added a dedicated preview workspace:
  - blank QuickClip surface
  - style-specific visual treatment (`is-style-1`, `is-style-2`, `is-style-3`)
  - live text rendering for Name 1 / Name 2 / Date
- Added Gemini result panel with generated summary output.
- Added generated QuickClip image output that composes the selected style image with generated preview text.
- Added `Generate` action between `Cancel` and `Save personalization`.

### State and validation behavior

- Added per-scope personalization state for PDP and quick-add contexts.
- Added live character counts for Name 1 and Name 2.
- Added validation for:
  - Name 1 max length
  - Name 2 max length
  - Date max length
  - required fields before Gemini generation
- Added trigger relabeling (`Customize Now` -> `Edit Customization`) when configured.
- Added error handling and loading state for Gemini generation.

### Line item property payload updates

- Kept compatibility properties:
  - `properties[Personalization Primary Text]`
  - `properties[Personalization Secondary Text]`
- Added expanded properties:
  - `properties[Personalization Style]`
  - `properties[Personalization Name 1]`
  - `properties[Personalization Name 2]`
  - `properties[Personalization Date]`
  - `properties[_Personalization Gemini Preview]`
  - `properties[_Personalization Scope]`

### Styling and layout updates

- Increased modal max width (`64rem` -> `72rem`).
- Converted fields area to a structured grid.
- Added style-tile component styles.
- Replaced simple preview surface with a two-column grid:
  - clip visualization column
  - Gemini output column
- Improved mobile behavior:
  - style options collapse to one column
  - preview grid collapses to one column
  - action buttons stretch full width

<!-- UI_CHANGES_AUTOGEN:START -->

### Auto-Generated UI Diff

Generated by `QuickClipsPersonalization/update-ui-readme.ps1` on 2026-03-07 19:46:18 -07:00.

Baseline commit: `f703355` (2026-03-07) - Add modular theme personalization previewer with line item property sync

Changed UI files: 4/5  
Total line delta: +866 / -110

| File | Status | +Lines | -Lines | Latest change |
| --- | --- | ---: | ---: | --- |
| `assets/personalization-preview.css` | Modified | 181 | 17 | `3a96b16` (2026-03-07) - Add style-aware QuickClip generation and Gemini 3.1 flash config |
| `assets/personalization-preview.js` | Modified | 549 | 79 | `3a96b16` (2026-03-07) - Add style-aware QuickClip generation and Gemini 3.1 flash config |
| `snippets/personalization-preview-modal.liquid` | Modified | 106 | 14 | `3a96b16` (2026-03-07) - Add style-aware QuickClip generation and Gemini 3.1 flash config |
| `snippets/personalization-line-item-properties.liquid` | Modified | 30 | 0 | `18bf934` (2026-03-07) - Integrate enhanced personalization preview workflow |
| `snippets/personalization-preview-trigger.liquid` | Unchanged | 0 | 0 | No changes since baseline |

<!-- UI_CHANGES_AUTOGEN:END -->

## Keep README Auto-Updated

Run this command to refresh the generated UI diff section:

```powershell
powershell -ExecutionPolicy Bypass -File QuickClipsPersonalization/update-ui-readme.ps1
```

Enable automatic refresh on every commit:

```powershell
git config core.hooksPath .githooks
```

With hooks enabled, the pre-commit hook updates this README from the baseline commit before each commit.

## Gemini Preview Server

This server handles Gemini preview generation so the API key is kept in environment variables.

### 1) Set environment variables

PowerShell example:

```powershell
$env:GEMINI_API_KEY = "YOUR_GEMINI_API_KEY"
$env:GEMINI_MODEL = "gemini-3.1-flash"   # optional
$env:PORT = "8788"                        # optional
```

### 2) Start the server

```powershell
node QuickClipsPersonalization/gemini-preview-server.mjs
```

API endpoint:

- `POST http://localhost:8788/api/personalization-preview`
- body: `{ "style": "Style 1", "name1": "...", "name2": "...", "date": "..." }`

### 3) Connect the theme UI

The theme JS checks `window.QuickClipsPersonalization.apiUrl`.

For local testing (browser console):

```js
window.QuickClipsPersonalization = {
  apiUrl: 'http://localhost:8788/api/personalization-preview',
};
```

If `window.QuickClipsPersonalization.apiUrl` is not set, the theme defaults to:

- `/apps/quickclips-personalization/preview`

Use that default path when routing through a Shopify app proxy in production.

## Style Button Images

Upload these three style thumbnails to your theme assets folder:

- `assets/quickclip-style-1.jpg`
- `assets/quickclip-style-2.jpg`
- `assets/quickclip-style-3.jpg`

The personalization modal uses those exact file names for the style image buttons.
